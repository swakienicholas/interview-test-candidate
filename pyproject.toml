[project]
name = "live-feed-interview"
version = "0.1.0"
description = "A small WebSocket debugging exercise for engineering interviews"
requires-python = ">=3.12,<3.13"
dependencies = [
    "websockets==15.0.1",
]

[tool.uv]
package = false
